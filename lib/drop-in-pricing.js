'use strict';

/**
 * Gym drop-in day pass — single source for Stripe amount (cents) and member-facing copy.
 */
const DROP_IN_AMOUNT_CENTS = 800; // $8.00
const DROP_IN_FEE_DISPLAY = '$8.00';
const DROP_IN_FEE_DISPLAY_SHORT = '$8';

module.exports = {
  DROP_IN_AMOUNT_CENTS,
  DROP_IN_FEE_DISPLAY,
  DROP_IN_FEE_DISPLAY_SHORT
};
